Q.1 What is List? How will you reverse a list?
Ans--> list is a collection data type which is represented by [] braces
    list which is orderable , indexable , mutable(changeble)
    list index always start from 0.

    Reverse List : print(list[::-1])

Q.3 Differentiate between append () and extend () methods?
Ans--> Append() : using of append method we can store single element in existing list.

       Extend() : using of extend method we can store more than one element in existing list (in the form of list)

Q.18 What is tuple? Difference between list and tuple.
Ans--> Tuple Is A Collection Data Type Which Is Contain Data Elements
       Tuple Which Is Represented By ()
       Tuple Which Is Orderable , Indexable And Immutable (can't changeble)

Q.43 Why Do You Use the Zip () Method in Python?
Ans--> zip() function creates an iterator that will aggregate elements from two or more iterables. 
       We can use the resulting iterator to quickly and consistently solve common programming problems,like creating dictionaries.


